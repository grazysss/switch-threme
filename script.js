function toggleMode() {
    const html = document.documentElement

    html.classList.toggle("light")
}

/* Abreviado...
Const definindo uma variável que vai ser trocada (html), 

html.classList
verificando a lista de classes existentes.

Toggle para mudar ao tema oposto!*/